SELECT B.HOUR AS HOUR, SUM(DECODE(ANIMAL_ID, NULL, 0, 1)) AS COUNT
  FROM ANIMAL_OUTS A RIGHT JOIN (
      SELECT LEVEL-1 AS HOUR FROM DUAL CONNECT BY LEVEL <= 24
  ) B ON TO_CHAR(A.DATETIME,'HH24') = B.HOUR
 GROUP BY B.HOUR
 ORDER BY B.HOUR;